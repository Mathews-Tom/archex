# M0.4 Final Verdict — Recover Context Economy and Promote a Verified Default

## Verdict: NO-GO

The explicit benchmark-only `archex_query_context_candidate` does not clear M0.4's first immutable 64-task frontier run. The gate failed before the required second same-revision passing run could exist. Default promotion, canonical-baseline promotion, release preparation, and M1 unblock are prohibited.

## Immutable evidence

- Source revision: `9f423fb60d62481e5cc22f5d5a4fb42cbf95d736`
- Task manifest digest: `0ae42fb18f96678b5e79591be8372d83fbeed646c4c08e7cc0f118eba4c2bd09`
- Candidate: `archex_query_context_candidate`
- Raw evidence manifest SHA-256: `0ecdb1dd372c30c64992979d32f6e0a7a44c4a08289cd56964d581145b671598`
- Checked-in reader artifact: `benchmarks/evidence/m0.4-context-candidate-run1.json`
- Evidence validation: `uv run archex benchmark validate --kind evidence --tasks-dir benchmarks/tasks --input .archex/m04-context-candidate-run3` exited 0 with 64 tasks and four strategies.
- Absolute gate: `uv run archex benchmark gate --input .archex/m04-context-candidate-run3 --min-recall 0.60 --min-f1 0.30 --min-mrr 0.55` exited 1 with 133 violations.

## Observed candidate result

Aggregate means clear the aggregate-looking rows but not the required per-task frontier: recall `0.990625`, precision `0.228816`, F1 `0.337067`, MRR `0.834459`, and completion-adjusted token efficiency `0.862607`.

The mandatory per-task rows fail in these counts:

| Requirement | Failing tasks |
| --- | ---: |
| Precision ≥ 0.20 | 38 |
| F1 ≥ 0.30 | 35 |
| MRR ≥ 0.55 | 16 |
| Completion-adjusted token efficiency ≥ 0.08 | 2 |
| Required-file recall regression | 0 |
| Region recall regression | 12 |
| Line recall regression | 19 |

The two zero-efficiency paths remain `loc_fastapi_solve_dependencies` and `loc_tokio_current_thread_block_on`. Candidate wall p95 is `3812.88 ms`, exceeding the `3000 ms` limit.

## Disposition

- `archex_query` remains the production default.
- `benchmarks/baseline.json` and canonical benchmark manifests remain unchanged.
- The candidate stack remains explicit and benchmark-only for follow-up investigation.
- No second passing-run claim exists; the failed first immutable run makes default promotion unavailable.
- M1 PRs #491–#494 and benchmark-quality drafts #495–#498 remain untouched and blocked.
- No M1-unblock record is issued. This verdict is the explicit negative unblock record: `m1_unblocked = false`.
- Release classification: no release. No CHANGELOG entry is warranted because no production default or canonical evidence changed.

## Review boundary

The published candidate stack is #510 → #511 → #512. No promotion PR exists because an atomic promotion would violate the gate. Review the M0.4 candidate changes and this NO-GO evidence; do not merge the stack as a substitute for a verified default.
